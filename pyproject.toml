[project]
name = "dns-bench"
version = "0.1.0"
description = "Async DNS benchmarking utility"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["rich>=13.0.0", "matplotlib>=3.7.0"]

[build-system]
requires = ["setuptools>=67"]
build-backend = "setuptools.build_meta"
